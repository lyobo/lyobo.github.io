---
title: 牛客刷题
date: 2019-09-17 18:43:38
tags: 树
categories: coding
comments: true
toc: true
---
## 求二叉树的最大深度
主要思想——递归
```c++
class Solution {
public:
    int maxDepth(TreeNode *root) {
        if(!root)
        {
            return 0;
        }
        int l=0,r=0;
        if(root->right)
        {
            r = maxDepth(root->right);
        }
        if(root->left)
        {
            l = maxDepth(root->left);
        }
        return max(l,r) + 1;
    }
};
```

## 求二叉树的最小深度
注意递归停止的条件，考虑左右节点均为空的情况。
```c++
class Solution {
public:
    int run(TreeNode *root) {
        if(!root)
        {
            return 0;
        }
        int l=0,r=0;
        if(root->right)
        {
            r = run(root->right);
        }
        if(root->left)
        {
            l = run(root->left);
        }
         if(l==0||r==0)
         {
            return 1+r+l;
         }
        return min(l,r) +1;
    }
};
```